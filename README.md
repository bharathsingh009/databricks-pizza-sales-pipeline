# databricks-pizza-sales-pipeline
A Databricks Medallion Architecture data pipeline built with Apache Spark (PySpark) combining a database menu and object store transactional logs into a self-updating dashboard[cite: 1].
